# ⚙️ 시스템 아키텍처 및 AI 파이프라인 (SYSTEM_ARCHITECTURE.md)

본 사양서는 **GCP(Google Cloud Platform) 및 Firebase 서버리스 인프라**를 기반으로 설계된 **실시간 AI 음성 인식, 화자 분석(Diarization), 맥락 요약** 파이프라인 및 모바일-웹 간의 유기적 데이터 연계(**Stitch API**) 설계 도면입니다.

---

## 0. 처리 모델 — 하이브리드 C (2026-05-23 결정)

비용 ÷ 정확도 ÷ 시연 어필의 균형점을 찾아 **하이브리드 C 모드**를 채택합니다.

| 처리 단계 | 위치 | 비용 |
|---|---|---|
| 녹음 + Opus 압축 | **모바일 단말** (RNNoise/AGC/AEC) | 무료 |
| 실시간 STT 피드 (시연용) | 단말 네이티브 STT 또는 mock | 무료 |
| **회의 종료 시**: 오디오 1회 업로드 | Cloud Storage | egress ~$0.003 |
| 본격 STT (화자분리 포함) | GCP STT v2 batch (asynchronous_recognize) | $0.016/분 |
| Gemini 요약 + 3중 검증 | Gemini 1.5 Pro (1회 호출) | $0.04 |
| **4중 사후 검수** (신규) | Gemini 1.5 Pro (1회 추가) | $0.05 |

**예상 비용 (2시간 회의 1건)**: 약 **$0.50 (≈ 700원)** — 전체 온라인 대비 25% 수준.

**Cloud Run keep-warm 제거**: WebSocket 스트리밍이 사라지므로 `--min-instances=0` 그대로 유지 가능. 첫 요청 Cold Start 1-3초는 회의록 업로드 시점이라 UX 영향 없음.

**시연 어필 유지**: 실시간 STT 채팅 풍선은 mock 시나리오로 그대로 보여줌. 실제 호출은 회의 끝난 후에만 발생.

---

## 1. 서버리스 인프라 구조 (Serverless Cloud Architecture)

비용을 최소화하면서 8개 단체의 대규모 동시 접속과 대용량 오디오 데이터 처리를 안정적으로 수행하기 위해 완벽한 **Event-Driven Serverless** 인프라를 채택합니다.

```
[ 모바일 WS 앱 ] ──────────(음성 스트리밍 / HTTPS)─────────┐
                                                            │
[ 웹 예약 시스템 ] ───────────(Firestore SDK)────────────┐   │
                                                        │   │
┌───────────────────────── Firebase Cloud ──────────────┼───┼──────────┐
│                                                       │   │          │
│   ┌───────────────┐      ┌─────────────────┐          │   │          │
│   │ Firebase Auth │      │  Cloud Storage  │◀─────────┼───┘          │
│   └───────┬───────┘      └────────┬────────┘          │              │
│           │                       │ (Upload Event Trigger)           │
│           ▼                       ▼                   ▼              │
│   ┌───────────────┐      ┌─────────────────┐      ┌───────────────┐  │
│   │   Firestore   │◀─────│ Cloud Functions │─────▶│    Express    │  │
│   │   NoSQL DB    │      │  (Node.js v20)  │      │   Stitch API  │  │
│   └───────────────┘      └────────┬────────┘      └───────────────┘  │
│                                   │                                  │
└───────────────────────────────────┼──────────────────────────────────┘
                                    │ (AI Pipeline Trigger)
                                    ▼
┌───────────────────────── Google Cloud Platform ──────────────────────┐
│                                                                      │
│   ┌───────────────────────────┐         ┌────────────────────────┐   │
│   │    Speech-to-Text API     │────────▶│     Gemini 1.5 Pro     │   │
│   │ (Speaker Diarization v2)  │         │ (Context Summarization)│   │
│   └───────────────────────────┘         └────────────────────────┘   │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 2. 실시간 AI 음성 및 요약 파이프라인 설계

모바일에서 수집되는 다중 화자 음성 데이터를 텍스트화하고 최종 요약본으로 가공하는 5단계 초정밀 프로세스 사양입니다.

### ① 원거리 미세 음성 부스팅 & 프리 프로세싱 (Audio Enhancement Subsystem)
* **목적**: 마이크와 대화자의 거리(최대 5m~8m)에 따른 감쇄 현상 및 에코/하울링을 극복하고, 멀리서 작게 말하는 발언을 최상의 감도로 추출합니다.
* **소프트웨어 파이프라인**:
  1. **AGC (Auto Gain Control)**: 아날로그 음성 파형 입력 즉시 이득을 자동 조절하여 멀리서 나는 미세 음성은 최대 `+18dB` 증폭하고, 가까운 소리는 감쇄하여 균일한 볼륨으로 평준화합니다.
  2. **AI Noise Suppression (RNNoise & NSNet2)**: 인공지능 기반의 실시간 잡음 제거 필터를 통과시켜 회의실 내부의 웅웅거리는 에어컨 소음, 종이 넘기는 바스락 소리 등을 데시벨 기준 `-25dB` 차단합니다.
  3. **AEC (Acoustic Echo Cancellation)**: 교육장 스피커 등에서 반사되는 울림 현상을 잔향 제거 알고리즘(Dereverberation)으로 실시간 정화합니다.

### ② 실시간 오디오 패킷 캡처 및 전송
* **코덱 프로파일**: 프리 프로세싱을 거친 고품질 오디오 데이터를 실시간 캡처하여 **Opus 코덱(Container: Ogg, 16kHz Sampling Rate, Mono, 24kbps)**으로 압축합니다.
* **프로토콜**: 실시간 텍스트 변환 피드를 지원하기 위해, 클라이언트 모바일 기기는 **Secure WebSockets (WSS)**를 통해 GCP Cloud Run에 탑재된 실시간 중계 서버와 연결을 맺고 오디오 바이너리 스트림을 전송합니다.

### ③ 실시간 STT 및 화자 분석 (GCP Speech-to-Text v2 & Voiceprint Matcher)
* **API 호출 설정**:
  ```json
  {
    "config": {
      "explicitDecodingConfig": {
        "encoding": "OGG_OPUS",
        "sampleRateHertz": 16000,
        "audioChannelCount": 1
      },
      "model": "telephony",
      "languageCodes": ["ko-KR"],
      "features": {
        "enableAutomaticPunctuation": true,
        "diarizationConfig": {
          "minSpeakerCount": 2,
          "maxSpeakerCount": 8,
          "enableSpeakerDiarization": true
        }
      }
    }
  }
  ```
* **동작원리 (성문 기반 화자 매칭)**:
  1. **GCP Speaker Diarization**: GCP STT v2가 음성의 주파수 특성을 바탕으로 발화자가 바뀌는 타이밍을 분할하여 일차적으로 화자 태그(`Speaker 1`, `Speaker 2`)를 부여합니다.
  2. **성문(Voiceprint) 대조 매칭**: 추출된 오디오 세그먼트에서 성문 임베딩 벡터를 실시간 연산하여, Firestore에 보관된 해당 단체 회원들의 **'성문 임베딩 데이터(Cosine Similarity 기준 85% 이상 일치)'**와 초고속 대조합니다.
  3. **매핑 완료**: 일치도가 가장 높은 사용자의 실명과 직책(예: "김청년 회장")을 자동으로 텍스트 버블과 결합하여 실시간 웹소켓 피드로 클라이언트에 즉각 송출합니다.

### ④ 담당자 무개입(Zero-Touch) 완성을 위한 '3중 안심 검증 엔진 (Assurance Engine)'
회의록 완성 후 담당자가 사후 교정을 하지 않도록, 백그라운드 서버리스 레이어에서 자동으로 무결성을 보장하는 **3단계 교차 검증 파이프라인**을 실행합니다.

```
[ STT 1차 원본 텍스트 ] 
       │
       ▼ (1단계: 성문 특징 & 디렉토리 매핑) 
┌────────────────────────────────────────────────────────┐
│  소속 단체(Organization)의 DB 회원 명부 및 역할 대조  │
│  * "부장님" / "회장님" 등 직함 기반 화자 정보 2차 보정 │
└────────────────────────────────────────────────────────┘
       │
       ▼ (2단계: Gemini 1.5 Pro Contextual Auto-Correction)
┌────────────────────────────────────────────────────────┐
│  앞뒤 문맥 흐름 분석을 통한 음성 오타 자가 치유       │
│  * "교육장 비원" -> "교육장 B1" (예약 스펙 대조 자동 정정) │
│  * "WS 장려회" -> "WS 장년회" (고유명사 문맥 매핑)   │
└────────────────────────────────────────────────────────┘
       │
       ▼ (3단계: 신뢰 점수(Confidence Score) 기반 분기 필터)
┌────────────────────────────────────────────────────────┐
│  Assurance Score 95% 이상 ──▶ [자동 종결 및 Stitch 배포]│
│  Assurance Score 95% 미만 ──▶ [모호 구간 Highlight Focus 제공] │
└────────────────────────────────────────────────────────┘
       │
       ▼ (4단계 — 신규: 사후 전체 재해석 검수 / Post-Meeting Audit)
┌────────────────────────────────────────────────────────┐
│  회의 종료 후 전체 오디오를 Gemini에 한 번에 입력         │
│  → 1차 산출 회의록(결정/액션) ↔ 4차 재해석 diff 비교    │
│  · 일치 항목: ✓ 무결 확정 스탬프                       │
│  · Pass 2 신규 발견: 골드 강조 + Add 버튼              │
│  · Pass 2 누락 항목: 빨간 ⚠ "맥락상 약함 — 검토 필요"   │
│  · 단어 변경: side-by-side diff                        │
│  형님이 [Pass 1 유지 / Pass 2 채택 / 병합] 선택 후 확정 │
└────────────────────────────────────────────────────────┘
```

### ④-1 사후 4중 검수 (Post-Meeting Audit Pass) — 회의 후 단 1회 추가 호출
* **트리거**: 회의 종료 + 사용자가 보고서 화면에서 `🔬 4중 검수 실행` 버튼 클릭
* **목적**: 실시간 처리 중에는 전체 맥락이 없었으므로 일부 결정/액션이 누락 또는 오해석될 수 있음. 끝난 후 처음부터 전체 오디오+텍스트를 한 번에 재해석해 1차 산출과 대조.
* **추가 비용**: 회의 1건당 ~$0.05 (Gemini 1.5 Pro 1회 호출). 무결성 확보 효과 대비 매우 저렴.
* **UI 결과 처리**:
  - 일치율 95%+ : 자동 "4중 검수 통과 골드 스탬프"
  - 신규 발견 항목: 사용자 1탭으로 회의록에 병합
  - 누락 의심 항목: 해당 `audioOffsetSeconds` 10초 클립 재생 버튼 제공

### ⑤ 비동기 Gemini 1.5 Pro 맥락 분석 및 요약 파이프라인
* **트리거 시점**: 사용자가 녹음을 종료(Commit)하면 Cloud Storage에 전체 오디오 및 풀 텍스트 대화록 데이터가 영구 백업되며, 동시에 **Cloud Functions**가 트리거되어 Gemini 1.5 Pro API를 호출합니다.
* **Gemini 시스템 프롬프트 규격**:
  ```markdown
  [역할 정의]
  너는 대한민국 단체 행정 문서 작성의 최고 권위자이자 전문 서기 비서이다.
  
  [입력 정보]
  1. 회의 기본 메타데이터 (스캔된 공문 요약 정보)
  2. 화자 분리된 실시간 대화록 텍스트 전체
  
  [처리 규칙]
  1. 대화록 전반의 맥락을 분석하여 회의 개요를 3줄 이내로 깔끔하게 기술하라.
  2. 회의 중 최종 합의 및 결정된 모든 핵심 안건을 포착하여 [결정사항] 리스트로 뽑아내라.
  3. 논의된 내용 중 즉시 실행해야 하는 일을 포착하고 담당자와 데드라인을 분석하여 [액션 아이템]을 지정하라.
  4. 중복 예약이나 불필요한 예약 경쟁으로 인한 비정상 회의록인지 분석하여 위험도를 리턴하라.
  
  [출력 형식] Strict JSON
  ```

---

## 3. Stitch API Specification (모바일-웹 연동 규격)

모바일에서 완성된 회의록 요약 보고서 정보를 웹 예약 시스템으로 즉각 전송 및 반영하는 연동 API 설계입니다.

### POST /api/v1/stitch/transfer
* **설명**: 모바일 WS 앱에서 요약된 결정사항 데이터를 통합 웹 예약 데이터베이스로 연계 등록합니다.
* **인증 프로토콜**: Bearer JWT (Firebase User Auth ID Token)

#### Request Header
```http
Authorization: Bearer <Firebase_ID_Token>
Content-Type: application/json
```

#### Request Body
```json
{
  "sourceMeetingId": "meet-982348",
  "orgId": "org1",
  "reservationDetails": {
    "title": "[회의연동] 6월 청년회 정기 체육대회 기획 모임",
    "targetDate": "2026-05-24",
    "timeSlot": {
      "start": "14:00",
      "end": "18:00"
    },
    "preferredRoom": "교육장 (B1)",
    "creator": "김청년 회장"
  },
  "summaryMetadata": {
    "keyDecision": "6월 13일 토요일 오후로 체육대회 개최일을 확정하고 교육장 B1을 즉시 예약하기로 결정함.",
    "actionAssignee": "박청년 총무"
  }
}
```

#### Response (201 Created)
```json
{
  "status": "success",
  "message": "Stitch 연동이 성공적으로 처리되었습니다. 예약이 즉시 예약되었습니다.",
  "data": {
    "reservationId": "res-st-884920",
    "assignedRoom": "교육장 (B1)",
    "date": "2026-05-24",
    "startTime": "14:00",
    "endTime": "18:00",
    "registeredAt": "2026-05-22T11:32:33Z"
  }
}
  ```json
  {
    "status": "error",
    "errorCode": "DUPLICATE_RESERVATION_DETECTED",
    "message": "AI 분석 결과, 이미 동일하거나 극도로 유사한 목적의 예약 건이 캘린더에 존재합니다. 중복 예약이 차단되었습니다."
  }
  ```

---

## 4. WS 영구 무결성/초고속 아키텍처 (Eternal Zero-Degradation Specs)

시간이 갈수록 시스템이 느려지고, 메모리가 꽉 차고, 에러가 전파되는 소프트웨어 노화(Software Aging) 현상을 근본적으로 차단하기 위해 설계된 **4대 영구 무결성 아키텍처 규격**입니다.

```
┌─────────────────────────────────────────────────────────────┐
│ [WS 에터널 가디언 아키텍처의 4대 순환 체계]                │
│                                                             │
│ 1. 🗄️ [GCP Data Lifecycle] ◀── 5년 경과 데이터 콜드 이관     │
│ 2. 📱 [Edge WebP Compression] ◀── 업로드 즉시 용량 80% 압축  │
│ 3. 💾 [Offline-First PWA Sync] ◀── 끊김 시 로컬 임시 자동 저장 │
│ 4. ⚡ [DOM Virtual Scroll] ◀── 1만 개 예약도 0.05초 만에 표출 │
└─────────────────────────────────────────────────────────────┘
```

### ① 데이터 에이징 해결: GCP 수명주기 데이터 다이어트 (Data Diet Pipeline)
* **문제점**: 8개 단체가 수년 동안 매주 사용하는 예약 기록, 회의록 텍스트, 첨부 사진 및 대용량 음성 녹음 파일이 메인 DB에 누적되면, 인덱스 검색 레이턴시가 폭증하고 검색 기능이 마비됩니다.
* **해결 방안**: 
  - **Firestore TTL(Time-to-Live) 정책**: 회의록과 예약의 `archivePolicy.expireAt`가 도래(작성 후 5년 경과)하면 Cloud Functions 배치 작업이 활성화되어 메인 Firestore에서 데이터를 자동 추출합니다.
  - **Cold Storage 자동 이관**: 추출된 데이터는 **GCP Cloud Storage Archive Class** 및 **BigQuery**의 아카이브 테이블로 비동기 이관되어 콜드 보관 처리됩니다.
  - **효과**: 실시간 런타임 Firestore의 데이터 크기가 항시 임계치(최근 3년 이내 핵심 데이터) 이하로 슬림하게 유지되어, 10년이 지나도 첫날과 완전히 동일한 **0.03초 이내의 응답 속도**를 영구 보장합니다.

### ② 업로드 병목 차단: 에지 단 WebP 초경량 변환 및 클라우드 안티바이러스 스캔
* **문제점**: 사용자들이 고화질(1장당 10MB 이상) 회의 화이트보드 판서 사진을 마구잡이로 업로드할 때 모바일 데이터가 폭증하고, 이미지 뷰어가 켜질 때 앱이 강제 종료되거나 서버 트래픽 비용이 폭증합니다.
* **해결 방안**:
  - **Edge-Compression (모바일 단 압축)**: 모바일 앱은 사진을 첨부하는 즉시, 업로드 전에 스마트폰 내부 GPU를 활용해 **최대 가로 1200px 해상도의 WebP 포맷(압축률 80% 설정)**으로 자동 리사이징합니다. (10MB 원본이 이미지 화질 손상 없이 300KB로 대폭 다이어트됨).
  - **Dual-URL (원본-썸네일 분리)**: 서버는 업로드된 이미지를 받아 즉시 50KB 용량의 썸네일(`thumbnailUrl`)을 추가 생성하여, 목록 캘린더 뷰어에서는 썸네일만 초고속 드로잉하게 하여 렌더링 무거움을 차단합니다.
  - **Cloud Anti-Virus Scan**: 첨부된 사진 파일이 웹 서버나 타 단원의 기기를 해킹하는 통로로 악용되지 않도록, Cloud Storage 업로드 이벤트 트리거 시점에 **GCP Cloud Functions 기반 ClamAV 이미지 백신 필터**를 즉각 태워 악성 스크립트를 원천 소독합니다.

### ③ 네트워크 음영 지역 극복: PWA 기반 Offline-First 동기화 아키텍처
* **문제점**: 지하실 교육장 등 통신망이 자꾸 끊기고 신호가 약한 장소에서 예약을 누르거나 회의록을 저장하려 할 때, 네트워크 시간 초과 에러(Timeout)가 나며 작성 중이던 모든 데이터가 소실되어 사용자 불만이 폭증합니다.
* **해결 방안**:
  - **로컬 임시 큐 적재 (Offline Queueing)**: 네트워크가 끊기면 앱이 통신 에러를 뿜는 대신, 모바일 기기의 로컬 암호화 스토리지(SQLite 및 IndexedDB)에 작성 중이던 대화록 및 첨부 사진을 임시 보관 상태로 즉각 세이브합니다.
  - **백그라운드 자동 Stitch 동기화**: 앱 내부의 백그라운드 서비스 워커(Service Worker)가 통신 강도를 3초마다 체크하여, 인터넷 신호가 안전 영역으로 복귀하면 조용히 로컬 임시 큐의 데이터를 클라우드로 자동 전송하여 병합(Stitch) 처리를 매끄럽게 완수합니다.

### ④ 화면 마비 방지: 윈도잉 기반 가상 스크롤(Virtual Scroll) 및 장애 격리
* **문제점**: 웹 예약 시스템 캘린더에 8개 단체의 10년 치 예약 블록이 빼곡하게 쌓여 로딩될 때, 브라우저가 수만 개의 HTML DOM 요소를 한꺼번에 렌더링하다 화면이 멈추거나 뻗어버립니다.
* **해결 방안**:
  - **Virtual Scrolling (가상 스크롤)**: 사용자가 보고 있는 현재 주/월 단위의 화면 영역(약 20~50개 예약 블록)만 브라우저 DOM에 렌더링하고, 화면 밖의 과거/미래 예약 요소는 메모리 상에 객체로만 들고 있는 윈도잉 기술을 강제합니다.
  - **서킷 브레이커(Circuit Breaker) 패턴**: 실시간 STT API나 외부 OCR API 서버 등 특정 AI 연동 모듈에 전 세계적 과부하가 생겨 다운되더라도, 기본 예약 시스템과 로컬 오디오 녹음 기능 등 핵심 캘린더 영역은 **독립된 마이크로서비스 인프라**로 가동하여 전체 시스템이 정지하는 연쇄적 에러 전파(Cascade Failure)를 완벽 격리합니다.

